doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        block title
            title Расширяемые поля

    body
        include ../common/mixins
        +header('Расширяемые поля')